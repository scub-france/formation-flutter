


expect(find.text('Hello, world!'), findsOneWidget);
});
}
//Ce test utilise la méthode pumpWidget() pour construire le widget de test,
// puis utilise la méthode expect() pour vérifier que le widget de texte affiche le texte "Hello, world!".